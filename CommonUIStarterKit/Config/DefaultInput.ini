; DefaultInput.ini — 입력 클래스 기본값 전용 파일.
;
; 학습 노트 (레퍼런스 §4·§10):
;   ⚠️ 이 파일은 legacy Action/Axis 매핑과 Enhanced Input 클래스 기본값 '전용'이다.
;   ⚠️ CommonUI/CommonInput 키(GameViewportClientClassName, CommonInputSettings 등)를
;      여기에 넣으면 '무시되어 무음 무효과'가 된다 → DefaultEngine.ini / DefaultGame.ini에 넣는다.
;
;   이 kit의 메뉴 입력은 CommonUI DataTable action 경로(FCommonInputActionDataBase +
;   UCommonUIInputData)로 배선하며(세션 C Stage 4), Enhanced Input은 gameplay 액션 전용이다.

[/Script/Engine.InputSettings]
; 세션 A: 골격만. 필요 시 세션 C에서 gameplay용 Enhanced Input 기본값 추가.
